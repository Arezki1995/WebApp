  let injectedData;

  var taskQueryResult= mysqlConnection.query('SELECT * FROM Tasks;', (err, result)=>{
  
  if(!err){
      injectedData.tasks=Object.assign({}, rows[0]);
    }else{
      console.log('error getting tasks');
    }
  });

  
  var newsQueryResult= mysqlConnection.query('SELECT * FROM News;', (err, result)=>{
    if(!err){
      injectedData.news=Object.assign({}, rows[0]);
    }else{
      console.log('error getting news');
    }
  });
  
  res.render('home',{injectedData:injectedData});  